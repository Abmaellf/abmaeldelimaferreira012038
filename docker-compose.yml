
services:
#  app-music:
#    build: .
#    env_file:
#      - .env
#    ports:
#      - ${APP_PORT_MUSIC}:${APP_PORT_MUSIC}
#
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:${DB_PORT_MUSIC}/${DB_NAME_MUSIC}
#      DB_USER_MUSIC: ${DB_USER_MUSIC}
#      DB_PASSWORD_MUSIC: ${DB_PASSWORD_MUSIC}
#      DB_NAME_MUSIC: ${DB_NAME_MUSIC}
#      DB_HOST_MUSIC: ${DB_HOST_MUSIC}
#      DB_PORT_MUSIC: ${DB_PORT_MUSIC}
#      DB_DDL_AUTO_MUSIC: ${DB_DDL_AUTO_MUSIC}
#      DB_DRIVER_MUSIC: ${DB_DRIVER_MUSIC}
#
#      SPRING_DATASOURCE_URL_MUSIC: ${SPRING_DATASOURCE_URL_MUSIC}
#      SPRING_DATASOURCE_USERNAME_MUSIC: ${SPRING_DATASOURCE_USERNAME_MUSIC}
#      SHOW_SQL_MUSIC: ${SHOW_SQL_MUSIC}
#
#      APP_PORT_MUSIC: ${APP_PORT_MUSIC}
#
#      JWT_SECRET_MUSIC: ${JWT_SECRET_MUSIC}
#      JWT_EXPIRATION_MUSIC: ${JWT_EXPIRATION_MUSIC}
#      JWT_REFRESH_EXPIRATION_MUSIC= ${JWT_REFRESH_EXPIRATION_MUSIC}
#     SEED_PASSWORD_ADMIN_MUSIC: ${SEED_PASSWORD_ADMIN_MUSIC}
#     SEED_EMAIL_ADMIN_MUSIC: ${SEED_EMAIL_ADMIN_MUSIC}
#     CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
#      JWT_REFRESH_EXPIRATION_MUSIC=500000
#      COOKIES_SECURE_MUSIC=true
#      SAME_SITE_MUSIC=None
#    networks:
#      - network-app-music
  postgres:
    image: postgres:15
    container_name: postgreSQL
    restart: always
    environment:
      POSTGRES_USER: "${DB_USER_MUSIC}"
      POSTGRES_PASSWORD: "${DB_PASSWORD_MUSIC}"
      POSTGRES_DB: "${DB_NAME_MUSIC}"
    ports:
      - "${DB_PORT_MUSIC}:${DB_PORT_MUSIC}"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER_MUSIC} -d ${DB_NAME_MUSIC}"]
      interval: 10s
      timeout: 5s
      retries: 5
    # opcional, mas útil para debug:
    logging:
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - network-app-music
volumes:
  postgres_data:
networks:
  network-app-music:
    driver: bridge